node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
.env
.DS_Store
*.log
.vite
.claude

# Personal photos — keep them out of the repo. Add your image to
# public/assets/ locally and point VITE_PHOTO at it; the badge shows an
# initials fallback when the photo is missing.
public/assets/*.jpg
public/assets/*.jpeg
public/assets/*.png
